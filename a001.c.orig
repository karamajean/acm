#include <stdio.h>
#include <stdlib.h>

int main()
{
    int num1,num2,num3;
    int length=0;
    int i;

    //while (scanf("%s", buffer)!=EOF) // 這個比較好
    while (scanf("%d %d", &num1,&num2)==2) // 引數配對為1
    {
        printf("num1 = %d\n", num1);
        printf("num2 = %d\n", num2);
        if ( num1 < 1 || num2 >= 10000  ) return 0;
        if ( num1 < num2 )
        {
            for (i=num1; i<num2; i++)
            {
                num3 = 0;
                if (num3==0) length +=1;

                while ( num3 != 1)
                {
                    if ( num3%2 == 0)
                    {
                        num3 /= 2;
                        length +=1;
                             }
                             if ( num3%2 == 1)
                    {
                        num3 = (num3*3)+1;
                        length +=1;
                             }
                         }


                     }
                 }
#if 0
                 if (num1 > num2)
        {
            for (i=num2; i<num1; i++)
            {


            }

        }
#endif
        pritnf("length = %d");
    }
    return 0;
}
